DOMAIN="http://localhost:3000"

# Database
DATABASE_URL="sqlite.db"

# Credentials (e.g. run `openssl rand -hex 32` to generate a strong secret)
JWT_SECRET="long-random-strong-string-as-a-serious-secret"

# Email
SMTP_HOST="smtp.example.com"
SMTP_FROM="Nodecho <no-reply@your-email.com>"
SMTP_SECURE=true
SMTP_PORT=465
SMTP_USER="username"
SMTP_PASS="password"
